
# The .env file is used by docker-compose to prevent hard-coding and reusable variables
# This can be modified and placed in the same directory as the docker-compose file when running
# Actual environment variables set in the shell will override these, if it is preferred to define that way at runtime
# eg. 1. export MYSQL_DATABASE=upperneno ; 2. docker-compose up
# The effects of these can be tested by running "docker-compose config"

MYSQL_DATA_DIR=~/environments/mysql/5.6/data
MYSQL_PORT=3308
MYSQL_DB_NAME=neno
MYSQL_USERNAME=root
MYSQL_PASSWORD=root

TOMCAT_PORT=8080
TOMCAT_DEBUG_PORT=5000
PWA_PORT=8081
GATEWAY_PORT=80

PATH_TO_WAR=~/code/github/pih/openmrs-module-pihmalawi/distro/target/distro/web/openmrs.war
PATH_TO_MODULES=~/openmrs/neno/modules
PATH_TO_CONFIG=~/code/github/pih/openmrs-module-pihmalawi/configuration/target/openmrs-packager-config/configuration
PATH_TO_PWA=~/code/github/pih/openmrs-pwa-apzu-ic3/build:/usr/share/nginx/html
OPENMRS_DATA_DIR=~/environments/neno/data
